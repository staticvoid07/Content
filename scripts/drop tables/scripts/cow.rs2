// https://oldschool.runescape.wiki/w/Cow#Drops
[ai_queue3,_cow]
gosub(npc_death);
if (npc_findhero = ^false) {
    return;
}
// Default drop from config.
inv_add(npc_param(death_drop), 1);

// Normal drop
inv_add(raw_beef, 1);
inv_add(cow_hide, 1);
