// https://oldschool.runescape.wiki/w/Cow#Drops
[ai_queue3,_cow]
gosub(npc_death);
if (npc_findhero = ^false) {
    return;
}
// Default drop from config.
obj_add(npc_coord, npc_param(death_drop), 1, ^lootdrop_duration);

// Normal drop
inv_add(inv, raw_beef, 1);
inv_add(inv, cow_hide, 1);
